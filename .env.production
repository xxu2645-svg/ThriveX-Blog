# 生产环境配置
# Zeabur 部署时请设置以下环境变量

# 项目后端API - Zeabur 会自动提供正确的服务地址
# 如果后端和前端部署在同一服务中，使用相对路径
NEXT_PUBLIC_PROJECT_API=/api

# 如果后端部署在不同的服务中，Zeabur 会提供环境变量
# NEXT_PUBLIC_PROJECT_API=${API_SERVICE_URL}/api

# 页面缓存时间（生产环境建议设置为 300 秒 = 5分钟）
NEXT_PUBLIC_CACHING_TIME=300